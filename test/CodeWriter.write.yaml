- codedict: 
    code : |
        def keyPressEvent(self, e):
        if e.key() == QtCore.Qt.Key_Escape:
            self.close()
  depth: 2
  expected: |4
              def keyPressEvent(self, e):
              if e.key() == QtCore.Qt.Key_Escape:
                  self.close()
